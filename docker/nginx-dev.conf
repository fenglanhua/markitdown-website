# nginx-dev.conf — 開發環境 Nginx 設定
# 與正式版 nginx.conf 的差異：
#   - root 改為 /usr/share/nginx/html（Docker 容器內路徑）
#   - 移除 server_name（開發不需要）
#   - 移除快取設定（開發時不快取）
#   - 保留所有 COOP/COEP headers（Pyodide 必要）

server {
    listen 80;

    root /usr/share/nginx/html;
    index index.html;

    # ── 開發環境：禁用瀏覽器快取 ─────────────────────────────────────────
    add_header Cache-Control "no-store, no-cache, must-revalidate" always;

    # ── 必要：SharedArrayBuffer 安全標頭 ──────────────────────────────────
    add_header Cross-Origin-Opener-Policy "same-origin" always;
    add_header Cross-Origin-Embedder-Policy "require-corp" always;

    # ── MIME Types ─────────────────────────────────────────────────────────
    # 與 nginx.conf（正式）相同的寫法：
    # include 產生第一個 types{}，下方再加第二個 types{}，同層級合併
    include /etc/nginx/mime.types;
    types {
        application/wasm                      wasm;
        application/octet-stream              whl;
    }

    # pyodide/ 和 wheels/ 靜態資源加上 CORP header（browser-sync 代理需要）
    location ~* ^/(pyodide|wheels)/ {
        add_header Cross-Origin-Opener-Policy "same-origin" always;
        add_header Cross-Origin-Embedder-Policy "require-corp" always;
        add_header Cross-Origin-Resource-Policy "same-origin" always;
    }

    # sw.js：Service Worker 腳本絕不可快取（含 4xx）
    location = /sw.js {
        expires -1;
        add_header Cache-Control "no-store, no-cache, must-revalidate" always;
        add_header Pragma "no-cache" always;
        add_header Cross-Origin-Opener-Policy  "same-origin"  always;
        add_header Cross-Origin-Embedder-Policy "require-corp" always;
    }

    # API proxy — 轉發至 proxy 容器
    location /api/ {
        proxy_pass http://proxy:3002/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 20s;
    }

    # 所有路由回到 index.html
    location / {
        try_files $uri $uri/ /index.html;
    }

    server_tokens off;
}
